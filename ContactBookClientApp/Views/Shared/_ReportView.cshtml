<div id="allEmployeesContent" class="card-header btn-light d-flex justify-content-between">
   
    <h3>Report</h3>
    
    <a id="BirthdatBasedReport" class="btn btn-primary" asp-controller="Report" asp-action="ContactRecordBasedOnBirthdayMonthReport">Birthday Based Report</a>
    <a class="btn btn-primary" asp-controller="Report" asp-action="GetContactsByStateReport">State Based Report</a>
    <a class="btn btn-primary" asp-controller="Report" asp-action="GetContactsCountByCountryReport">Country Based Report</a>
    <a class="btn btn-primary" asp-controller="Report" asp-action="GetContactCountByGenderReport">Gender Based Report</a>

</div>

<style>
    /* Ensure the body and HTML have no padding or margin */
    /* html, body {
                            margin: 0;
                            padding: 0;
                            width: 100%;
                            height: 100%;
                            overflow-x: hidden; /* Prevent horizontal scrolling */
    }
    */
    /* .container {
                            display: flex;
                            flex-direction: column; /* Layout columns vertically */
    align-items: center; /* Center items horizontally */
    gap: 20px; /* Spacing between elements */
    padding: 20px;
    box-sizing: border-box; /* Ensure padding does not affect width */
    }

    */

    #allEmployeesContent {
        display: flex;
        padding: 10px;
        border-bottom: solid 0.2px lightgray;
        align-items: center; /* Align items vertically */
        gap: 10px; /* Spacing between heading and buttons */
    }

    .button-group {
        display: flex; /* Display buttons in a row */
        gap: 10px; /* Adjust spacing between buttons */
    }

        .button-group a {
            padding: 10px 20px;
            border: none;
            background-color: #007bff;
            color: #fff;
            border-radius: 5px;
            text-decoration: none; /* Remove underline from links */
            cursor: pointer;
        }
</style>



<script>
    $(document).ready(function () {
        initialLoad();
    });

    function initialLoad() {
        document.getElementById('BirthdatBasedReport').click();
    };
</script>